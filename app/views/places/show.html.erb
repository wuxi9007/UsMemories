<div>
  <p><%= @place.name %> <%= @place.latitude %>,<%= @place.longitude %> <%= link_to '<span class="fa fa-edit">'.html_safe, edit_place_path(@place)  %></p>
  <div>
    <% @place.images.each do |i| %>
      <%= image_tag i.drive_url, style: "width:200px" %>
    <% end %>
  </div>

</div>
